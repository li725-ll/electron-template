appId: "you appid"
productName: "demo"

nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  installerIcon: '' # 安装程序的图标

#{
#  "from": "path/to/source",
#  "to": "path/to/destination",
#  "filter": [ "**/*", "!foo/*.js" ]
#}
extraResources: # 要复制到资源文件夹的文件，例如：图片

extraFiles: #要复制到程序根文件夹的文件，例如：dll文件

win: #windows的单独配置

mac: #mac的单独配置


